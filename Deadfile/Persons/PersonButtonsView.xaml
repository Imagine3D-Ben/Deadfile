<UserControl x:Class="Deadfile.Persons.PersonButtonsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:service="clr-namespace:Deadfile.Services"
             service:DialogService.IsRegisteredView="True"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Margin="5" Command="{Binding NewPersonCommand}">New person</Button>
                <Button Margin="5" Command="{Binding EditPersonCommand}">Edit person</Button>
                <Button Margin="5" Command="{Binding DeletePersonCommand}">Delete person</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
